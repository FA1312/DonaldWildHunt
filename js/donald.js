class Player {
  constructor(x, y, radius, color, image) {
    this.x = x;
    this.y = y;
    this.radius = radius;
    this.color = color;
    this.image = new Image();
    this.image.src = image;
  }
  
}